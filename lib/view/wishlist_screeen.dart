import 'package:flutter/material.dart';

class WishlistScreeen extends StatelessWidget {
  const WishlistScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Wishlist Screen'),
      ),
    );
  }
}